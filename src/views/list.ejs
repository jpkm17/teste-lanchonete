<%- include('partials/head.ejs') %>    
    <h1>Listagem de cliente</h1>

    <table border="1">
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Telefone</th>
            <th>Endereço</th>
        </tr>
        <% users.forEach(user => { %>
            <tr>
                <td> <%= user.name %> </td>
                <td> <%= user.email %> </td>
                <td> <%= user.phone %> </td>
                <td> <%= user.address %> </td>
                <td>
                    <a href="/listCustomer/remove/<%=user._id%>">Deletar</a>
                </td>
            </tr>
        <% }) %>

    </table>
<%- include('partials/foot.ejs') %>